# Optional dependencies for Databricks-backed providers only.
# Install on top of requirements.txt when storage.provider: unity_catalog
# and/or approval.provider: ontobricks. Not needed for local dev - the
# databricks.sql import in src/providers/_delta_sql.py is deferred until
# a Delta-backed provider is actually instantiated.
databricks-sql-connector>=3.1.0
