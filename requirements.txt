docling>=2.15.0,<3.0.0
neo4j>=5.20.0,<6.0.0
python-dotenv>=1.0.1,<2.0.0
PyYAML>=6.0.1,<7.0.0
rdflib==7.6.0
# Pinned to the current stable release (3.8.16, 2026-08-24) - no pretrained
# language model is downloaded, extraction.spacy_entity_extractor only uses
# spacy.blank("en") + a sentencizer + an EntityRuler seeded from ontology.yaml.
spacy==3.8.16
tiktoken>=0.7.0,<1.0.0
tqdm>=4.66.4,<5.0.0
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pytest>=8.0,<9.0
agent-framework>=1.0.0,<2.0.0
# agent-framework-azure-ai has no stable >=1.0.0 release yet (only rc/beta
# builds) - pin the latest release candidate explicitly so `pip install -r
# requirements.txt` succeeds without --pre. Bump this pin as newer rc/stable
# builds ship.
agent-framework-azure-ai==1.0.0rc6
